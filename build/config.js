// 打包生成目录的名字
const APP_FOLDER_NAME = "vue-app";
// 个人配置文件名
const PERSONAL_FILE_NAME = "personal-config.json";

const Config = {
    APP_FOLDER_NAME,
    PERSONAL_FILE_NAME
};

module.exports = Config;
